<h1>These are all the bands in the database</h1>

<ul>
<% @bands.each do |band| %>
    <li><a href="<%= band_url(band.id) %>"><%=band.name %></li>
<% end %>
</ul>

<a href="<%= new_band_url %>">Add a new band</a>